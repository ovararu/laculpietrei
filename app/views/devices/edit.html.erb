<% content_for :title, "Edit #{@device.name}" %>

<div class="max-w-2xl">
  <div class="mb-6">
    <%= link_to "← Back", device_path(@device), class: "text-sm text-gray-500 hover:text-blue-600" %>
  </div>

  <div class="bg-white rounded-xl shadow-sm border border-gray-200 px-6 py-6">
    <h1 class="text-xl font-bold text-gray-900 mb-6">Edit <%= @device.name %></h1>
    <%= render "form", device: @device %>
  </div>
</div>
